# Scenario evaluation fixtures

Deterministic fixtures are defined by the evaluation tests. They cover valid and invalid
negative results, unresolved refs, positive-chain assumptions, bounded TTC evidence, and
the current `scenario-evidence-v1` mixed-kind limitation.
